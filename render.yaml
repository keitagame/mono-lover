services:
  - type: web
    name: mono-lover
    env: node
    region: singapore   # ← 日本から最も近いリージョン
    buildCommand: "npm install"
    startCommand: "npm start"
    plan: free
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
